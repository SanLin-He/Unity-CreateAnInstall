using UnityEngine;
using System.Collections;

public class Main : MonoBehaviour
{
    public TextAsset cube; 
	// Use this for initialization
	void Start () {
	
        //生成设备唯一码文件，并拷贝到指定地方。


	}
	
	// Update is called once per frame
	void Update () {
	
	}

    public void StartInstall(string path)
    {
        //选择位置（使用默认位置）
        //解压工程文件夹
        //将工程文件夹拷贝到指定路径
    }
}
